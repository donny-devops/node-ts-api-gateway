interface:
  display_name: "Node Ts Api Gateway"
  short_description: "Repo-specific patterns and workflows for node-ts-api-gateway"
  default_prompt: "Use the node-ts-api-gateway repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true